import 'package:flutter/material.dart';

class ProductDetilsScr extends StatelessWidget {
  const ProductDetilsScr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
