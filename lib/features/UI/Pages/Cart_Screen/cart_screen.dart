import 'package:flutter/material.dart';

class CartScr extends StatelessWidget {
  const CartScr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
